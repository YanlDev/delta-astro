---
// Hero.astro
// Importa la imagen que usarás
import heroImage from "../../assets/img/hero-img.jpg";
---

<section
  class="relative bg-gradient-to-r from-slate-900 to-slate-800 overflow-hidden"
>
  <!-- Patrón de fondo -->
  <div class="absolute inset-0 opacity-10">
    <div
      class="absolute -top-24 -right-24 w-96 h-96 bg-indigo-500 rounded-full mix-blend-multiply blur-2xl"
    >
    </div>
    <div
      class="absolute top-0 -left-24 w-72 h-72 bg-blue-500 rounded-full mix-blend-multiply blur-2xl"
    >
    </div>
    <div
      class="absolute bottom-0 right-10 w-80 h-80 bg-purple-500 rounded-full mix-blend-multiply blur-2xl"
    >
    </div>
  </div>

  <!-- Grid de fondo -->
  <div class="absolute inset-0 opacity-5">
    <div class="h-full w-full grid grid-cols-12 gap-1">
      {
        Array(12)
          .fill(0)
          .map((_, i) => <div class="h-full border-r border-gray-300" />)
      }
    </div>
    <div class="h-full w-full grid grid-rows-6 gap-1">
      {
        Array(6)
          .fill(0)
          .map((_, i) => <div class="w-full border-b border-gray-300" />)
      }
    </div>
  </div>

  <!-- Contenido principal -->
  <div class="container mx-auto px-4 py-16 sm:py-24 relative z-10">
    <div class="flex flex-col lg:flex-row items-center">
      <!-- Textos y CTA -->
      <div class="lg:w-1/2 lg:pr-12 mb-12 lg:mb-0 text-center lg:text-left">
        <h5 class="text-blue-400 font-medium mb-3 tracking-wider">
          SERVICIOS ACADÉMICOS PROFESIONALES
        </h5>
        <h1
          class="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6"
        >
          Transforma tus ideas en <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-indigo-500"
            >excelencia académica</span
          >
        </h1>
        <p class="text-lg text-gray-300 mb-8 max-w-2xl mx-auto lg:mx-0">
          Ofrecemos asesoría especializada en tesis, investigaciones y trabajos
          académicos con la garantía de alta calidad y originalidad verificable.
        </p>
        <div
          class="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-4"
        >
          <a
            href="#servicios"
            class="bg-blue-600 hover:bg-blue-700 text-white font-medium px-8 py-3 rounded-md transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg w-full sm:w-auto text-center"
          >
            Nuestros Servicios
          </a>
          <a
            href="#contacto"
            class="bg-transparent border border-white/30 hover:border-white/80 text-white font-medium px-8 py-3 rounded-md transition-all duration-300 hover:bg-white/10 w-full sm:w-auto text-center"
          >
            Consulta Gratuita
          </a>
        </div>
        
      </div>

      <!-- Imagen Hero -->
      <div class="lg:w-1/2 relative">
        <div
          class="relative mx-auto lg:ml-auto lg:mr-0 w-full max-w-md lg:max-w-lg"
        >
          <!-- Marco decorativo -->
          <div
            class="absolute inset-0 border-2 border-blue-400/30 rounded-lg transform translate-x-4 translate-y-4"
          >
          </div>

          <!-- Contenedor de la imagen -->
          <div
            class="relative bg-white/5 backdrop-blur-sm border border-white/20 rounded-lg shadow-2xl p-2 overflow-hidden"
          >
            <!-- Superposición de gradiente -->
            <div
              class="absolute inset-0 bg-gradient-to-tr from-blue-600/40 to-transparent z-10 mix-blend-overlay"
            >
            </div>

            <!-- Imagen -->
            <img
              src={heroImage.src}
              alt="Estudiantes graduados celebrando su éxito académico"
              class="w-full h-full object-cover rounded-md relative z-0"
            />

            <!-- Elementos superpuestos -->
            <div
              class="absolute top-4 right-4 bg-blue-600 text-white text-xs font-bold px-3 py-1 rounded-full z-20 shadow-lg"
            >
              100% Originalidad
            </div>

            <!-- Tarjeta de testimonio -->
            <div
              class="absolute -bottom-0 -left-0 bg-white/90 rounded-lg shadow-xl p-4 max-w-xs z-20"
            >
              <div class="flex items-start gap-3">
                <div
                  class="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center flex-shrink-0"
                >
                  <svg
                    class="w-6 h-6 text-blue-600"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                    xmlns="http://www.w3.org/2000/svg"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M5 13l4 4L19 7"></path>
                  </svg>
                </div>
                <div>
                  <div class="text-yellow-500 flex mb-1">
                    {
                      [1, 2, 3, 4, 5].map((i) => (
                        <svg
                          class="w-4 h-4"
                          fill="currentColor"
                          viewBox="0 0 20 20"
                          xmlns="http://www.w3.org/2000/svg"
                        >
                          <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                        </svg>
                      ))
                    }
                  </div>
                  <p class="text-slate-700 text-sm font-medium">
                    "Mi tesis fue aprobada con honores gracias a su asesoría
                    profesional."
                  </p>
                  <p class="text-slate-500 text-xs mt-1">
                    María G. - Ingeniería
                  </p>
                </div>
              </div>
            </div>
          </div>

          <!-- Elementos decorativos -->
          <div
            class="absolute -bottom-10 -right-10 w-20 h-20 bg-gradient-to-br from-indigo-600 to-purple-600 rounded-full blur-md opacity-50 z-0"
          >
          </div>
          <div
            class="absolute -top-4 right-20 w-8 h-8 bg-blue-500 rounded-full blur-sm opacity-70 z-0"
          >
          </div>
          <div
            class="absolute top-1/2 -right-6 w-12 h-12 bg-indigo-500 rounded-full blur-sm opacity-70 z-0"
          >
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
